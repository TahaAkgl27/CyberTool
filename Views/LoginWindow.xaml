<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="CyberTool.Views.LoginWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="CyberTool Giriş">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="1" Spacing="20" Margin="40" VerticalAlignment="Center">
            
            <!-- Header / Logo Area -->
            <StackPanel Spacing="10" HorizontalAlignment="Center" Margin="0,0,0,20">
                <FontIcon Glyph="&#xE77B;" FontSize="48" FontFamily="Segoe MDL2 Assets" Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"/>
                <TextBlock Text="CyberTool" Style="{StaticResource TitleTextBlockStyle}" HorizontalAlignment="Center"/>
                <TextBlock Text="Güvenlik Test Platformu" Style="{StaticResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" HorizontalAlignment="Center"/>
            </StackPanel>

            <!-- Login Form -->
            <TextBox x:Name="UsernameBox" Header="Kullanıcı Adı" PlaceholderText="kullanıcı adı" />
            <PasswordBox x:Name="PasswordBox" Header="Şifre" PlaceholderText="••••••••" PasswordChar="•" />

            <InfoBar x:Name="ErrorBar" IsOpen="False" Severity="Error" Message="Kullanıcı adı veya şifre hatalı." />

            <Button x:Name="LoginButton" Content="Giriş Yap" Click="LoginButton_Click" HorizontalAlignment="Stretch" Style="{StaticResource AccentButtonStyle}" />
            
        </StackPanel>
        
        <!-- Footer -->
        <TextBlock Grid.Row="2" Text="© 2025 CyberTool" VerticalAlignment="Bottom" HorizontalAlignment="Center" Margin="20" Style="{StaticResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorDisabledBrush}"/>
    </Grid>
</Window>
