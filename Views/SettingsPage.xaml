<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="CyberTool.Views.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CyberTool.Views">

    <Grid Padding="24" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Spacing="4">
            <TextBlock Text="Ayarlar" FontSize="24" FontWeight="SemiBold" />
            <TextBlock Text="Uygulama tercihleri ve API yapılandırması." Opacity="0.75" />
        </StackPanel>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Spacing="16">
                <Border Padding="16" CornerRadius="12" Background="{ThemeResource ControlFillColorDefaultBrush}">
                    <StackPanel Spacing="12">
                        <TextBlock Text="OpenAI API Anahtarı" FontWeight="SemiBold" />
                        <TextBlock Text="Yapay zeka analizi ve otomatik düzeltme scriptleri için OpenAI API anahtarınızı girin. Anahtar yalnızca yerel olarak saklanır." TextWrapping="Wrap" Opacity="0.7" />
                        <PasswordBox x:Name="ApiKeyBox" Header="API Anahtarı" PlaceholderText="sk-..." PasswordChar="•" />
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <Button x:Name="SaveApiKeyButton" Content="Kaydet" Click="SaveApiKeyButton_Click" Style="{StaticResource AccentButtonStyle}" />
                            <Button x:Name="ClearApiKeyButton" Content="Temizle" Click="ClearApiKeyButton_Click" />
                        </StackPanel>
                        <InfoBar x:Name="ApiKeyStatusBar" IsOpen="False" Severity="Informational" />
                    </StackPanel>
                </Border>

                <Border Padding="16" CornerRadius="12" Background="{ThemeResource ControlFillColorDefaultBrush}">
                    <StackPanel Spacing="12">
                        <TextBlock Text="Görünüm" FontWeight="SemiBold" />
                        <TextBlock Text="Buraya ileride tema seçimi (Açık/Koyu/Sistem) gibi seçenekler eklenecek." TextWrapping="Wrap" Opacity="0.7" />
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
