<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="CyberTool.Views.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CyberTool.Views"
    Background="{StaticResource CyberPageBackground}">

    <Grid Padding="32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Spacing="6" Margin="0,0,0,24">
            <TextBlock Text="Ayarlar" Style="{StaticResource PageTitleStyle}"/>
            <TextBlock Text="Uygulama tercihleri, güvenlik ve API yapılandırması." Style="{StaticResource PageSubtitleStyle}"/>
        </StackPanel>

        <Grid Grid.Row="1" ColumnSpacing="24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="220"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Settings nav -->
            <Border Style="{StaticResource ModernCardStyle}" Padding="12" VerticalAlignment="Top">
                <StackPanel Spacing="4">
                    <TextBlock Text="GENEL" Style="{StaticResource CyberLabelStyle}" Margin="8,4,8,8"/>
                    <Button HorizontalAlignment="Stretch" HorizontalContentAlignment="Left" Background="{StaticResource CyberSidebarActiveBrush}" Foreground="{StaticResource CyberAccentBrush}" BorderThickness="0" CornerRadius="10" Padding="12,10">
                        <TextBlock Text="Genel" FontWeight="SemiBold"/>
                    </Button>
                    <Button HorizontalAlignment="Stretch" HorizontalContentAlignment="Left" Style="{StaticResource CyberSecondaryButtonStyle}" BorderThickness="0" Background="Transparent" Padding="12,10">
                        <TextBlock Text="Tarama"/>
                    </Button>
                    <Button HorizontalAlignment="Stretch" HorizontalContentAlignment="Left" Style="{StaticResource CyberSecondaryButtonStyle}" BorderThickness="0" Background="Transparent" Padding="12,10">
                        <TextBlock Text="Güvenlik"/>
                    </Button>
                    <Button HorizontalAlignment="Stretch" HorizontalContentAlignment="Left" Style="{StaticResource CyberSecondaryButtonStyle}" BorderThickness="0" Background="Transparent" Padding="12,10">
                        <TextBlock Text="AI / Analiz"/>
                    </Button>
                    <Button HorizontalAlignment="Stretch" HorizontalContentAlignment="Left" Style="{StaticResource CyberSecondaryButtonStyle}" BorderThickness="0" Background="Transparent" Padding="12,10">
                        <TextBlock Text="Raporlar"/>
                    </Button>
                    <Button HorizontalAlignment="Stretch" HorizontalContentAlignment="Left" Style="{StaticResource CyberSecondaryButtonStyle}" BorderThickness="0" Background="Transparent" Padding="12,10">
                        <TextBlock Text="Görünüm"/>
                    </Button>
                    <Button HorizontalAlignment="Stretch" HorizontalContentAlignment="Left" Style="{StaticResource CyberSecondaryButtonStyle}" BorderThickness="0" Background="Transparent" Padding="12,10">
                        <TextBlock Text="Geçmiş"/>
                    </Button>
                </StackPanel>
            </Border>

            <!-- Settings content -->
            <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Auto">
                <StackPanel Spacing="20">

                    <Border Style="{StaticResource ModernCardStyle}">
                        <StackPanel Spacing="16">
                            <StackPanel Spacing="4">
                                <TextBlock Text="OpenAI API Anahtarı" FontSize="16" FontWeight="SemiBold"/>
                                <TextBlock Text="Yapay zeka analizi ve otomatik düzeltme scriptleri için API anahtarınızı girin. Anahtar yalnızca yerel olarak saklanır." TextWrapping="Wrap" Foreground="{StaticResource CyberTextSecondaryBrush}"/>
                            </StackPanel>
                            <PasswordBox x:Name="ApiKeyBox" Header="API Anahtarı" PlaceholderText="sk-..." PasswordChar="•" CornerRadius="10"/>
                            <StackPanel Orientation="Horizontal" Spacing="10">
                                <Button x:Name="SaveApiKeyButton" Content="Kaydet" Click="SaveApiKeyButton_Click" Style="{StaticResource CyberPrimaryButtonStyle}"/>
                                <Button x:Name="ClearApiKeyButton" Content="Temizle" Click="ClearApiKeyButton_Click" Style="{StaticResource CyberSecondaryButtonStyle}"/>
                                <Button Content="Bağlantıyı Test Et" Style="{StaticResource CyberSecondaryButtonStyle}"/>
                            </StackPanel>
                            <InfoBar x:Name="ApiKeyStatusBar" IsOpen="False" Severity="Informational"/>
                        </StackPanel>
                    </Border>

                    <Border Style="{StaticResource ModernCardStyle}">
                        <StackPanel Spacing="12">
                            <TextBlock Text="Görünüm" FontSize="16" FontWeight="SemiBold"/>
                            <TextBlock Text="Tema seçimi ve arayüz tercihleri." Foreground="{StaticResource CyberTextSecondaryBrush}"/>
                            <ComboBox Header="Tema" SelectedIndex="0" HorizontalAlignment="Stretch" CornerRadius="10">
                                <ComboBoxItem Content="Sistem"/>
                                <ComboBoxItem Content="Koyu"/>
                                <ComboBoxItem Content="Açık"/>
                            </ComboBox>
                            <ToggleSwitch Header="Kompakt tablo görünümü" OffContent="Kapalı" OnContent="Açık"/>
                        </StackPanel>
                    </Border>

                    <Border Style="{StaticResource ModernCardStyle}">
                        <StackPanel Spacing="12">
                            <TextBlock Text="Tarama" FontSize="16" FontWeight="SemiBold"/>
                            <NumberBox Header="Varsayılan port başlangıç" Value="1" Minimum="1" Maximum="65535" SpinButtonPlacementMode="Compact" CornerRadius="10"/>
                            <NumberBox Header="Varsayılan port bitiş" Value="1000" Minimum="1" Maximum="65535" SpinButtonPlacementMode="Compact" CornerRadius="10"/>
                        </StackPanel>
                    </Border>

                    <Border Style="{StaticResource ModernCardStyle}">
                        <StackPanel Spacing="12">
                            <TextBlock Text="Güvenlik" FontSize="16" FontWeight="SemiBold"/>
                            <ToggleSwitch Header="Lab modu (yalnızca özel IP aralıkları)" OffContent="Kapalı" OnContent="Açık"/>
                            <ToggleSwitch Header="Yüksek riskli işlemlerde onay iste" OffContent="Kapalı" OnContent="Açık" IsOn="True"/>
                        </StackPanel>
                    </Border>

                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Grid>
</Page>
