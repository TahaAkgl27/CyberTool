<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="CyberTool.Views.DeviceProfilePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CyberTool.Views"
    xmlns:vm="using:CyberTool.ViewModels"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:converters="using:CyberTool.Converters"
    mc:Ignorable="d">

    <Page.Resources>
        <vm:DeviceViewModel x:Key="ViewModel"/>
        <converters:SeverityColorConverter x:Key="SeverityConverter" />
    </Page.Resources>

    <ScrollViewer DataContext="{StaticResource ViewModel}">
        <StackPanel Padding="24" Spacing="24">
            
            <!-- 1. HERO HEADER -->
            <Grid>
                <StackPanel Spacing="8">
                    <StackPanel Orientation="Horizontal" Spacing="12">
                        <FontIcon Glyph="&#xE950;" FontSize="28" Foreground="{ThemeResource SystemAccentColor}"/>
                        <TextBlock Text="{Binding Profile.DeviceName}" FontSize="28" FontWeight="Bold"/>
                        <TextBlock Text="{Binding Profile.IpAddress}" FontSize="20" Opacity="0.5" VerticalAlignment="Bottom" Margin="4,0,0,2"/>
                    </StackPanel>
                    
                    <StackPanel Orientation="Horizontal" Spacing="12">
                        <Border Background="#15FFFFFF" CornerRadius="16" Padding="12,4">
                            <TextBlock Text="{Binding Profile.Status}" FontWeight="SemiBold"/>
                        </Border>
                        <TextBlock Text="Son Görülme:" Opacity="0.5" VerticalAlignment="Center"/>
                        <TextBlock Text="{Binding Profile.LastSeen, Converter={StaticResource StringFormatConverter}, ConverterParameter='{}{0:dd.MM.yyyy HH:mm}'}" Opacity="0.8" VerticalAlignment="Center"/>
                    </StackPanel>
                </StackPanel>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Spacing="12" VerticalAlignment="Center">
                    <!-- Deep Scan Login Area -->
                    <Button Content="🔒 Giriş Yap &amp; Doğrula" Style="{StaticResource AccentButtonStyle}" Background="#3498DB">
                        <Button.Flyout>
                            <Flyout>
                                <StackPanel Spacing="12" Width="240">
                                    <TextBlock Text="Yönetici Doğrulaması" FontWeight="Bold" FontSize="16"/>
                                    <TextBlock Text="Derin analiz (WMI/WinRM) için yetkili hesap bilgileri girin." FontSize="11" Opacity="0.7" TextWrapping="Wrap"/>
                                    
                                    <TextBox Header="Kullanıcı Adı" PlaceholderText="örn: Administrator" Text="{Binding DeepScanUsername, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                                    <PasswordBox Header="Parola" Password="{Binding DeepScanPassword, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                                    
                                    <Button Content="Analizi Başlat" Command="{Binding RunDeepScanCommand}" HorizontalAlignment="Stretch" Style="{StaticResource AccentButtonStyle}"/>
                                    <ProgressBar IsIndeterminate="True" Visibility="{Binding IsDeepScanning, Converter={StaticResource BoolToVisibilityConverter}}" Height="2"/>
                                </StackPanel>
                            </Flyout>
                        </Button.Flyout>
                    </Button>
                    
                    <Button Content="Yönetici Özeti (PDF)" Style="{StaticResource AccentButtonStyle}" Background="#30E74C3C"/> 
                    <Button Content="Teknik Detay (CSV)"/>
                    <Button Content="Kontrol Listesi"/>
                </StackPanel>
            </Grid>

            <!-- 2. KPI CARDS (Rich Discovery) -->
            <Grid ColumnSpacing="16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- System Discover Card -->
                <Border Grid.Column="0" Style="{StaticResource ModernCardStyle}" Padding="16">
                    <StackPanel Spacing="8">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <FontIcon Glyph="&#xE777;" FontSize="16" Opacity="0.7"/>
                            <TextBlock Text="SİSTEM KEŞFİ" FontSize="11" FontWeight="Bold" Opacity="0.6"/>
                        </StackPanel>
                        <TextBlock Text="{Binding Profile.WindowsVersion}" FontSize="13" FontWeight="SemiBold" TextWrapping="NoWrap"/>
                        <TextBlock Text="{Binding Profile.OsBuild}" FontSize="11" Opacity="0.7"/>
                        <TextBlock Text="Factual (Deep)" FontSize="11" Foreground="#2ECC71" FontWeight="Bold" Margin="0,4,0,0"/>
                    </StackPanel>
                </Border>

                <!-- Hardware Analayis Card -->
                <Border Grid.Column="1" Style="{StaticResource ModernCardStyle}" Padding="16">
                    <StackPanel Spacing="8">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <FontIcon Glyph="&#xE950;" FontSize="16" Opacity="0.7"/>
                            <TextBlock Text="DONANIM ANALİZİ" FontSize="11" FontWeight="Bold" Opacity="0.6"/>
                        </StackPanel>
                        <TextBlock Text="{Binding Profile.TotalRamGb, Converter={StaticResource StringFormatConverter}, ConverterParameter='{}RAM: {0} GB'}" FontSize="13" FontWeight="SemiBold"/>
                        <TextBlock Text="{Binding Profile.CpuModel}" FontSize="10" FontWeight="Bold" Foreground="#3498DB" TextWrapping="NoWrap"/>
                        <TextBlock Text="{Binding Profile.Manufacturer}" FontSize="10" Opacity="0.6"/>
                    </StackPanel>
                </Border>

                <!-- Security Posture Card -->
                <Border Grid.Column="2" Style="{StaticResource ModernCardStyle}" Padding="16">
                    <StackPanel Spacing="8">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <FontIcon Glyph="&#xE72E;" FontSize="16" Opacity="0.7"/>
                            <TextBlock Text="GÜVENLİK DURUŞU" FontSize="11" FontWeight="Bold" Opacity="0.6"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <TextBlock Text="🛡️ Defender" FontSize="12" Opacity="0.9"/>
                            <TextBlock Text="✅" FontSize="12" Foreground="Green"/>
                            <TextBlock Text="🧱 Firewall" FontSize="12" Opacity="0.9" Margin="8,0,0,0"/>
                            <TextBlock Text="✅" FontSize="12" Foreground="Green"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Spacing="8">
                             <TextBlock Text="🔐 BitLocker" FontSize="12" Opacity="0.9"/>
                             <TextBlock Text="❌" FontSize="12" Foreground="Red"/>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- Software/Updates Card -->
                <Border Grid.Column="3" Style="{StaticResource ModernCardStyle}" Padding="16">
                    <StackPanel Spacing="8" VerticalAlignment="Center">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <FontIcon Glyph="&#xE74C;" FontSize="16" Opacity="0.7"/>
                            <TextBlock Text="YAZILIM &amp; GÜNCELLEME" FontSize="11" FontWeight="Bold" Opacity="0.6"/>
                        </StackPanel>
                        <TextBlock Text="{Binding Profile.Hotfixes.Count, Converter={StaticResource StringFormatConverter}, ConverterParameter='{}{0} Yüklü Yama'}" FontSize="13" FontWeight="SemiBold"/>
                        <StackPanel Orientation="Horizontal" Spacing="4">
                             <TextBlock Text="Son Yama:" FontSize="10" Opacity="0.6"/>
                             <TextBlock Text="KB5034441" FontSize="10" FontWeight="Bold" Foreground="#2ECC71"/>
                        </StackPanel>
                        <ProgressBar Value="100" Foreground="#2ECC71" Height="4" Margin="0,4,0,0"/>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- 3. AI SUMMARY -->
            <Border Background="#10AA0000" BorderBrush="#30E74C3C" BorderThickness="1" CornerRadius="12" Padding="24">
                <Grid ColumnSpacing="24">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <Grid Width="100" Height="100">
                        <Ellipse Stroke="#10FFFFFF" StrokeThickness="8"/>
                        <TextBlock Text="{Binding Profile.PostureScore}" FontSize="32" FontWeight="Bold" Foreground="{Binding Profile.PostureScore, Converter={StaticResource SeverityConverter}, ConverterParameter=Foreground}" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,0,0,8"/>
                        <TextBlock Text="PUAN" FontSize="10" Opacity="0.5" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,32,0,0"/>
                    </Grid>

                    <StackPanel Grid.Column="1" Spacing="12">
                         <StackPanel Orientation="Horizontal" Spacing="8">
                             <FontIcon Glyph="&#xE9D9;" Foreground="#E74C3C" FontSize="18"/>
                             <TextBlock Text="AI '10 SANİYE' ÖZETİ" FontWeight="Bold" Foreground="#E74C3C" CharacterSpacing="100"/>
                         </StackPanel>
                         
                         <TextBlock Text="{Binding Profile.ExecutiveSummary}" FontSize="15" FontWeight="SemiBold"/>
                         
                         <Grid ColumnSpacing="24">
                             <Grid.ColumnDefinitions>
                                 <ColumnDefinition Width="*"/>
                                 <ColumnDefinition Width="*"/>
                                 <ColumnDefinition Width="*"/>
                             </Grid.ColumnDefinitions>
                             
                             <StackPanel Spacing="4">
                                 <TextBlock Text="Neden Riskli?" FontSize="11" FontWeight="Bold" Opacity="0.6"/>
                                 <TextBlock Text="{Binding Profile.TopRiskFactors}" TextWrapping="Wrap" FontSize="13" Opacity="0.9"/>
                             </StackPanel>
                             
                             <StackPanel Grid.Column="1" Spacing="4">
                                 <TextBlock Text="En Hızlı Kazanım (15 dk)" FontSize="11" FontWeight="Bold" Opacity="0.6"/>
                                 <TextBlock Text="{Binding Profile.QuickWinAction}" TextWrapping="Wrap" FontSize="13" Foreground="#2ECC71" FontWeight="SemiBold"/>
                             </StackPanel>

                             <StackPanel Grid.Column="2" Spacing="4">
                                 <TextBlock Text="Yapılmazsa Ne Olur?" FontSize="11" FontWeight="Bold" Opacity="0.6"/>
                                 <TextBlock Text="{Binding Profile.BusinessImpact}" TextWrapping="Wrap" FontSize="13" Opacity="0.9" FontStyle="Italic"/>
                             </StackPanel>
                         </Grid>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- 4. PIVOT: DETAILS (Console-Style and Fact-Style) -->
            <Pivot x:Name="DetailPivot">
                <PivotItem Header="Sistem Kimliği">
                    <StackPanel Spacing="16" Margin="0,16,0,0">
                         <Border Background="#05FFFFFF" Padding="16" CornerRadius="8">
                             <StackPanel Spacing="12">
                                 <Grid>
                                    <Grid.ColumnDefinitions><ColumnDefinition Width="180"/><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                                    <TextBlock Text="Windows Sürümü" Opacity="0.6"/>
                                    <TextBlock Grid.Column="1" Text="{Binding Profile.WindowsVersion}" FontWeight="Bold"/>
                                    <FontIcon Grid.Column="2" Glyph="&#xE946;" FontSize="14" Opacity="0.4"/>
                                 </Grid>
                                 <Grid>
                                    <Grid.ColumnDefinitions><ColumnDefinition Width="180"/><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                                    <TextBlock Text="Build / Versiyon" Opacity="0.6"/>
                                    <TextBlock Grid.Column="1" Text="{Binding Profile.OsBuild}" FontWeight="Bold"/>
                                 </Grid>
                                 <Grid>
                                    <Grid.ColumnDefinitions><ColumnDefinition Width="180"/><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                                    <TextBlock Text="Domain / Grup" Opacity="0.6"/>
                                    <TextBlock Grid.Column="1" Text="{Binding Profile.Domain}" FontWeight="Bold" Foreground="#3498DB"/>
                                 </Grid>
                                 <Grid>
                                    <Grid.ColumnDefinitions><ColumnDefinition Width="180"/><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                                    <TextBlock Text="Kurulum Tarihi" Opacity="0.6"/>
                                    <TextBlock Grid.Column="1" Text="{Binding Profile.InstallDate}" FontWeight="Bold"/>
                                 </Grid>
                                 <Grid>
                                    <Grid.ColumnDefinitions><ColumnDefinition Width="180"/><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                                    <TextBlock Text="Son Önyükleme" Opacity="0.6"/>
                                    <TextBlock Grid.Column="1" Text="{Binding Profile.BootTime}" FontWeight="Bold"/>
                                 </Grid>
                             </StackPanel>
                         </Border>
                    </StackPanel>
                </PivotItem>

                <PivotItem Header="Donanım &amp; Ağ">
                    <StackPanel Spacing="16" Margin="0,16,0,0">
                         <Border Background="#05FFFFFF" Padding="16" CornerRadius="8">
                             <StackPanel Spacing="12">
                                 <Grid>
                                    <Grid.ColumnDefinitions><ColumnDefinition Width="180"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                                    <TextBlock Text="Manufacturer" Opacity="0.6"/>
                                    <TextBlock Grid.Column="1" Text="{Binding Profile.Manufacturer}" FontWeight="Bold"/>
                                 </Grid>
                                 <Grid>
                                    <Grid.ColumnDefinitions><ColumnDefinition Width="180"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                                    <TextBlock Text="Model" Opacity="0.6"/>
                                    <TextBlock Grid.Column="1" Text="{Binding Profile.Model}" FontWeight="Bold"/>
                                 </Grid>
                                 <Grid>
                                    <Grid.ColumnDefinitions><ColumnDefinition Width="180"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                                    <TextBlock Text="Processor(s)" Opacity="0.6"/>
                                    <TextBlock Grid.Column="1" Text="{Binding Profile.CpuModel}" FontWeight="Bold" FontSize="12"/>
                                 </Grid>
                                 <Grid>
                                    <Grid.ColumnDefinitions><ColumnDefinition Width="180"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                                    <TextBlock Text="Memory (RAM)" Opacity="0.6"/>
                                    <TextBlock Grid.Column="1" Text="{Binding Profile.TotalRamGb, Converter={StaticResource StringFormatConverter}, ConverterParameter='{}{0} GB'}" FontWeight="Bold"/>
                                 </Grid>
                                 <Grid>
                                    <Grid.ColumnDefinitions><ColumnDefinition Width="180"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                                    <TextBlock Text="Network (IPConfig)" Opacity="0.6"/>
                                    <ItemsControl Grid.Column="1" ItemsSource="{Binding Profile.NetworkInterfaces}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding}" FontSize="12" FontWeight="SemiBold" Margin="0,0,0,4"/>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                 </Grid>
                             </StackPanel>
                         </Border>
                    </StackPanel>
                </PivotItem>

                <PivotItem Header="Yazılım &amp; Güncellemeler">
                     <Grid ColumnSpacing="24" Margin="0,16,0,0">
                         <Grid.ColumnDefinitions>
                             <ColumnDefinition Width="*"/>
                             <ColumnDefinition Width="*"/>
                         </Grid.ColumnDefinitions>

                         <StackPanel Spacing="12">
                             <TextBlock Text="Yüklü Güvenlik Yamaları (Hotfixes)" FontSize="14" FontWeight="SemiBold" Opacity="0.8"/>
                             <ListView ItemsSource="{Binding Profile.Hotfixes}" SelectionMode="None">
                                 <ListView.ItemTemplate>
                                     <DataTemplate>
                                         <Border Padding="12" Background="#05FFFFFF" Margin="0,0,0,8" CornerRadius="4">
                                             <StackPanel Orientation="Horizontal" Spacing="8">
                                                 <FontIcon Glyph="&#xE73E;" FontSize="14" Foreground="#2ECC71"/>
                                                 <TextBlock Text="{Binding}" FontWeight="Bold"/>
                                             </StackPanel>
                                         </Border>
                                     </DataTemplate>
                                 </ListView.ItemTemplate>
                             </ListView>
                         </StackPanel>

                         <StackPanel Grid.Column="1" Spacing="12">
                             <TextBlock Text="Kurumsal Yazılım Envanteri" FontSize="14" FontWeight="SemiBold" Opacity="0.8"/>
                             <ListView ItemsSource="{Binding Profile.InstalledSoftwares}" SelectionMode="None">
                                 <ListView.ItemTemplate>
                                     <DataTemplate>
                                         <Border Padding="12" Background="#05FFFFFF" Margin="0,0,0,8" CornerRadius="4">
                                              <TextBlock Text="{Binding}" FontSize="12" Opacity="0.9"/>
                                         </Border>
                                     </DataTemplate>
                                 </ListView.ItemTemplate>
                             </ListView>
                         </StackPanel>
                     </Grid>
                </PivotItem>

                <PivotItem Header="Komut Çıktısı (Raw)">
                    <Border Background="#10000000" Padding="16" Margin="0,16,0,0" CornerRadius="8" BorderBrush="#20FFFFFF" BorderThickness="1">
                         <ScrollViewer>
                             <TextBlock Text="{Binding Profile.RawCommandOutput}" FontFamily="Consolas" FontSize="13" Foreground="#2ECC71"/>
                         </ScrollViewer>
                    </Border>
                </PivotItem>

                <PivotItem Header="Servisler &amp; Portlar">
                    <ListView ItemsSource="{Binding Profile.Findings}" SelectionMode="None" Margin="0,16,0,0">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Border Background="#05FFFFFF" Padding="12" CornerRadius="8" Margin="0,0,0,8">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="80"/>
                                            <ColumnDefinition Width="120"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="{Binding Port}" FontWeight="Bold" VerticalAlignment="Center"/>
                                        <Border Grid.Column="1" Background="#203498DB" CornerRadius="4" Padding="6,2" HorizontalAlignment="Left">
                                            <TextBlock Text="{Binding Service}" FontSize="10" FontWeight="Bold" Foreground="#3498DB"/>
                                        </Border>
                                        <TextBlock Grid.Column="2" Text="{Binding Product}" Opacity="0.8" FontSize="13" VerticalAlignment="Center"/>
                                        <TextBlock Grid.Column="3" Text="{Binding State}" Foreground="#2ECC71" FontWeight="Bold"/>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </PivotItem>
            </Pivot>

        </StackPanel>
    </ScrollViewer>
</Page>
